# validation/

Purged & embargoed walk-forward splitting, sample-uniqueness weighting,
CPCV, and the permutation/placebo sanity checks (README.md section 4).
This is the highest-priority module to get right before anything else —
build and unit-test this before training a single real model.
