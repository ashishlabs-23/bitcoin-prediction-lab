# experiments/

Experiment log (one record per run: feature set, target, model, validation
scheme, costs, result — README.md section 8) and the flagship ablation
study by data source (README.md section 10). Start here once validation/
and models/ have a working baseline ladder — this is the highest-value
experiment in the whole project.
